const arrow=`<svg viewBox="0 0 18 18" aria-hidden="true"><path d="M4 14L14 4M7 4h7v7" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="square"/></svg>`;
function enhance(){
 const shell=document.querySelector('.shell');if(!shell)return;
 if(!shell.querySelector('.mobileBar')){const bar=document.createElement('div');bar.className='mobileBar';bar.innerHTML=`<div class="mobileLogo">SYSTEM<sup>©</sup></div><button type="button" class="burger" aria-label="Menu"><span></span><span></span><span></span></button>`;const backdrop=document.createElement('div');backdrop.className='menuBackdrop';shell.prepend(backdrop);shell.prepend(bar);bar.querySelector('.burger').addEventListener('click',e=>{e.preventDefault();e.stopPropagation();document.body.classList.toggle('menuOpen')});backdrop.addEventListener('click',()=>document.body.classList.remove('menuOpen'));shell.querySelectorAll('[data-route]').forEach(b=>b.addEventListener('click',()=>document.body.classList.remove('menuOpen')))}
 shell.querySelectorAll('.nav button').forEach(b=>{const spans=b.querySelectorAll('span');if(spans[1]&&!spans[1].querySelector('svg')){spans[1].innerHTML=arrow;spans[1].classList.add('navArrow')}});
 const prev=document.getElementById('prev'),next=document.getElementById('next');if(prev&&!prev.querySelector('.mobileNavArrow')){prev.setAttribute('aria-label','Previous section');prev.innerHTML=`<span class="desktopNavLabel">← PREV</span><span class="mobileNavArrow"></span>`}if(next&&!next.querySelector('.mobileNavArrow')){next.setAttribute('aria-label','Next section');next.innerHTML=`<span class="desktopNavLabel">NEXT →</span><span class="mobileNavArrow"></span>`}
}
const root=document.getElementById('app');if(root)new MutationObserver(()=>requestAnimationFrame(enhance)).observe(root,{childList:true,subtree:true});enhance();
